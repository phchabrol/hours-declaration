services:
  hours-declaration:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hours-declaration
    ports:
      - "3000:80"
    restart: unless-stopped
    volumes:
      # Optional: mount a volume for data persistence if needed
      - ./data:/app/data

